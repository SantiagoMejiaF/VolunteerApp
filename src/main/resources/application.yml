server:
  port: 8085
  servlet:
    context-path: /api/v1/user-management

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: volunteer-app-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/VolunteerAppDB
    username: postgres
    password: fcHRH2TPJTPKmbd3
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

    hibernate:
      ddl-auto: update
    show-sql: true

request-mapping:
  controller:
    user: users
    volunteer: volunteers
